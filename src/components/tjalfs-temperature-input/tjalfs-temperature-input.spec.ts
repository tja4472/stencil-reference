// import { flush, render } from '@stencil/core/testing';
import { TemperatureInput } from './tjalfs-temperature-input';

describe('tjalfs-temperature-input', () => {
  it('should build', () => {
    expect(new TemperatureInput()).toBeTruthy();
  });
});
