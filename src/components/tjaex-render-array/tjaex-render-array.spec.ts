// import { flush, render } from '@stencil/core/testing';
import { RenderArray } from './tjaex-render-array';

describe('tjaex-render-array', () => {
    it('should build', () => {
      expect(new RenderArray()).toBeTruthy();
    });
}); 
