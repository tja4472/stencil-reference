// import { flush, render } from '@stencil/core/testing';
import { PropDecorator } from './tjaex-prop-decorator';

describe('tjaex-prop-decorator', () => {
    it('should build', () => {
      expect(new PropDecorator()).toBeTruthy();
    });
}); 
