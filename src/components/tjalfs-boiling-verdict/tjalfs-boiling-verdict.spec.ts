// import { flush, render } from '@stencil/core/testing';
import { BoilingVerdict } from './tjalfs-boiling-verdict';

describe('tjalfs-boiling-verdict', () => {
  it('should build', () => {
    expect(new BoilingVerdict()).toBeTruthy();
  });
});
