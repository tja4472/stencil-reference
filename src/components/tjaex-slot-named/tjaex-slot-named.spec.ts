// import { flush, render } from '@stencil/core/testing';
import { SlotNamed } from './tjaex-slot-named';

describe('tjaex-slot', () => {
    it('should build', () => {
      expect(new SlotNamed()).toBeTruthy();
    });
}); 
