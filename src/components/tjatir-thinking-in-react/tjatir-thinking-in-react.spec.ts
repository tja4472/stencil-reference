// import { flush, render } from '@stencil/core/testing';
import { ThinkingInReact } from './tjatir-thinking-in-react';

describe('tjatir-thinking-in-react', () => {
    it('should build', () => {
      expect(new ThinkingInReact()).toBeTruthy();
    });
}); 
