// import { flush, render } from '@stencil/core/testing';
import { Comment } from './tjaex-comment';

describe('tjaex-comment', () => {
    it('should build', () => {
      expect(new Comment()).toBeTruthy();
    });
}); 
