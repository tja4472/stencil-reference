// import { flush, render } from '@stencil/core/testing';
import { DataBinding } from './tjaex-data-binding';

describe('tjaex-data-binding', () => {
    it('should build', () => {
      expect(new DataBinding()).toBeTruthy();
    });
}); 
