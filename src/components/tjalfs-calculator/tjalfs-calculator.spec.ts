// import { flush, render } from '@stencil/core/testing';
import { Calculator } from './tjalfs-calculator';

describe('tjalfs-calculator', () => {
  it('should build', () => {
    expect(new Calculator()).toBeTruthy();
  });
});
