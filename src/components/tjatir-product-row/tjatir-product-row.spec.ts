// import { flush, render } from '@stencil/core/testing';
import { ProductRow } from './tjatir-product-row';

describe('tjatir-product-row', () => {
    it('should build', () => {
      expect(new ProductRow()).toBeTruthy();
    });
}); 
