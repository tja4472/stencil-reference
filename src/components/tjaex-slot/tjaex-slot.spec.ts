// import { flush, render } from '@stencil/core/testing';
import { Slot } from './tjaex-slot';

describe('tjaex-slot', () => {
    it('should build', () => {
      expect(new Slot()).toBeTruthy();
    });
}); 
