// import { flush, render } from '@stencil/core/testing';
import { ProductTable } from './tjatir-product-table';

describe('tjatir-product-table', () => {
    it('should build', () => {
      expect(new ProductTable()).toBeTruthy();
    });
}); 
